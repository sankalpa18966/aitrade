version: '3.8'

services:
  forex_ai:
    build: .
    container_name: forex_ai_bot
    restart: unless-stopped
    # Uncomment the following lines if you want to keep the logs and news cache persistent on your host machine
    # volumes:
    #   - ./seen_news.json:/app/seen_news.json
    #   - ./errors.log:/app/errors.log
